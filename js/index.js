/** @format */

"use strict";

let tabBTN = documnet.querySelectorAll(".choose");
let tabs = document.querySelectorAll(".features-content__tabs-block__tab-show");

tabBTN.forEach(function (element) {
	element.addEventListener("click", () => {
        
    });
});
